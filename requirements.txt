# FinScope 依赖清单
# 安装: pip install -r requirements.txt

# ---- 金融数据 ----
tushare>=1.4.0
akshare>=1.16.0

# ---- PDF 解析 ----
# MinerU 为独立可选依赖（pip install mineru），或部署 mineru-api 服务（推荐，见 docker-compose）
# PyMuPDF 已按产品决策移除（解析质量优先，无 MinerU 时明确报错而非低质量降级）
requests>=2.31.0

# ---- 上下文压缩（P1 地基） ----
lxml>=5.2          # 表格 HTML -> 结构化网格（rowspan/colspan 展开，容错不规范标签）
tiktoken>=0.8      # 精确 token 计数（预算驱动装配的正确性前提）

# ---- LangChain & LangGraph ----
langchain>=1.0.5
langchain-core>=1.0.0
langchain-community>=0.4.1
langchain-openai>=1.0.2
langchain-ollama>=1.0.0
langgraph>=1.0.0
langgraph-checkpoint-sqlite>=2.0.1

# ---- Web 框架 ----
streamlit>=1.52.0

# ---- 配置 ----
pydantic>=2.10.1
pydantic-settings>=2.12.0
python-dotenv>=1.0.1

# ---- 数据处理 ----
numpy>=1.24
pandas>=2.2.3
